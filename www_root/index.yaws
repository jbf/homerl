</head>
<body>
<h1>Sensors</h1>
<erl>
    out(Arg) ->
        Sensors = sensor_directory:get_sensors(),
        {html, ["<table>",
                ["<tr><td><a href=/sensor.yaws?id=" ++
                 integer_to_list(Id) ++
                 ">" ++
                 integer_to_list(Id) ++
                 ", " ++
                 atom_to_list(Type) ++
                 "</a></td></tr>\n" || {_, Id, Type} <- Sensors],
               "</table>"]}.
</erl>

</body>
</html>
