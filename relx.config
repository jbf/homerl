{release, {data_logger_release, "1"},
  [lager,
   data_logger,
   device_directory,
   device_control,
   inets,
   ssl,
   push_client
  ]}.
{extended_start_script, true}.
{sys_config, "rel/sys.config"}.
{vm_args, "rel/vm.args"}.
{overlay, [{mkdir, "data"}]}.
